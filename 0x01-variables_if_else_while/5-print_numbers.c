/*
 * File: 5-print_numbers.c
 * Auth: Isah Abdulmumin
 */

#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase, except for q and e.
 *
 * Return: Always 0.
 */
int main(void)
{
	int n;

	for (n = 0; letter < 10; n++)
	{
		printf("%d\n", n);
	}

	return (0);
}
